enum ShockType {
    Left = 'left',
    Right = 'right',
    Top = 'top',
    Bottom = 'bottom',
    Back = 'back',
    Front = 'front',
}

/**
 * **DevShock - Device shocks detector**
 *
 */
export default class DevShock {

}

(async () => {
    console.log('hi')
})()
